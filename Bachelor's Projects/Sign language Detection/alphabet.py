#!/usr/bin/env python
# coding: utf-8

# In[2]:



import cv2



choice = ''
while choice != 'exit':
    choice = input('Enter alphabet\n')
    
    if choice=='a':        
        img_grayscale = cv2.imread('data/train/A/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('A',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='b':        
        img_grayscale = cv2.imread('data/train/B/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('B',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='c':        
        img_grayscale = cv2.imread('data/train/C/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('C',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='d':        
        img_grayscale = cv2.imread('data/train/D/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('D',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='e':        
        img_grayscale = cv2.imread('data/train/E/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('E',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='f':        
        img_grayscale = cv2.imread('data/train/F/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('F',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='e':        
        img_grayscale = cv2.imread('data/train/E/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('E',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='f':        
        img_grayscale = cv2.imread('data/train/F/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('F',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='g':        
        img_grayscale = cv2.imread('data/train/G/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('G',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='h':        
        img_grayscale = cv2.imread('data/train/H/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('H',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='i':        
        img_grayscale = cv2.imread('data/train/I/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('I',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='j':        
        img_grayscale = cv2.imread('data/train/J/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('J',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='k':        
        img_grayscale = cv2.imread('data/train/K/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('K',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='l':        
        img_grayscale = cv2.imread('data/train/L/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('L',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='m':        
        img_grayscale = cv2.imread('data/train/M/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('M',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='n':        
        img_grayscale = cv2.imread('data/train/N/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('N',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='o':        
        img_grayscale = cv2.imread('data/train/O/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('O',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='p':        
        img_grayscale = cv2.imread('data/train/P/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('P',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='q':        
        img_grayscale = cv2.imread('data/train/Q/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('Q',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='r':        
        img_grayscale = cv2.imread('data/train/R/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('R',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='s':        
        img_grayscale = cv2.imread('data/train/S/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('S',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='t':        
        img_grayscale = cv2.imread('data/train/T/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('T',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='u':        
        img_grayscale = cv2.imread('data/train/U/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('U',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='v':        
        img_grayscale = cv2.imread('data/train/V/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('V',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='w':        
        img_grayscale = cv2.imread('data/train/W/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('W',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='x':        
        img_grayscale = cv2.imread('data/train/X/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('X',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='y':        
        img_grayscale = cv2.imread('data/train/Y/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('Y',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()
    if choice=='z':        
        img_grayscale = cv2.imread('data/train/Z/0.jpg',cv2.THRESH_BINARY)
        resized = cv2.resize(img_grayscale, (320,320), interpolation = cv2.INTER_AREA)
        cv2.imshow('Z',resized)
        cv2.waitKey(0)
        cv2.destroyAllWindows()


# In[ ]:




